meta {
  name: Get User Organizations
  type: http
  seq: 2
}

get {
  url: {{SUPABASE_API_URL}}/functions/v1/data-api/v1/organizations/{{organization_id}}/users
  body: none
  auth: none
}

auth:bearer {
  token: 
}

vars:pre-request {
  ~organization_id: <set value and enable to overwrite default>
}
