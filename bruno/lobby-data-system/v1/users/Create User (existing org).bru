meta {
  name: Create User (existing org)
  type: http
  seq: 4
}

post {
  url: {{SUPABASE_API_URL}}/functions/v1/data-api/v1/users
  body: json
  auth: bearer
}

auth:bearer {
  token: {{SUPABASE_SERVICE_ROLE_KEY}}
}

body:json {
  {
      "organization_id": "{{organization_id}}"
  }
}

vars:pre-request {
  ~organization_id: 
}
